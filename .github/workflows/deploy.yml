name: Deploy to Cloudflare Workers (disabled)

# Disabled until CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID secrets are
# configured at https://github.com/wsokolowski-hub/SOLENCL/settings/secrets/actions
# Re-enable by uncommenting the `on:` block below.

on:
  workflow_dispatch:

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
